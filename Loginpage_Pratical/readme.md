For this project we can install (express, mongooes, expressmon, ejs)

for install type in terminal
(npm install express mongooes expressmon ejs)


for show our project we can check on browser
(localhost:8000)
